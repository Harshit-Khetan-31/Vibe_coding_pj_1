/**
 * INTRO's own content: the short boot line. The name and statement already
 * live in `profile.ts` and are reused as-is rather than duplicated here.
 */

export const boot = {
  // TODO(phase-3): confirm wording. Kept short on purpose — it has to clear
  // in ≤1.5s (CLAUDE.md: "a short console-style boot line on first load").
  line: 'HUD BOOT · INSTRUMENTS OK · READY FOR TAXI',
}
