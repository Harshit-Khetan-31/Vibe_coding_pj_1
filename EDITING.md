# Editing content

All text lives in `src/content/*.ts`. Edit those files — you never need to touch the
component/JSX files to change wording.

- `profile.ts` — name, hero statement, section labels
- More files land in Phase 3/4: `work.ts`, `experiments.ts`, `about.ts`, `logbook.ts`
